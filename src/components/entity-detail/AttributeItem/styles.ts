import { StyleSheet } from 'react-native'

export default StyleSheet.create({
	expressionItemDescription: {
		display: 'flex',
		flexDirection: 'row',
		alignItems: 'center',
	},
	iconExpression: { marginRight: -10 },
	lockIcon: {
		transform: [{ scale: 0.8 }],
	},
})
